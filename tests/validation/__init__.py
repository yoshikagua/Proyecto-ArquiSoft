"""Validation tests for static config and repository synchronization.

These tests cover docker-compose alignment, environment examples and
notification request validation without requiring services to run.

Ejecutar: pytest tests/validation/ -v
"""
