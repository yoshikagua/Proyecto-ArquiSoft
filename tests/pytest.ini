[pytest]
markers =
    integration: integration tests that require a subset of live services
    e2e: end-to-end tests that require the full stack or clean availability checks
    validation: static validation tests for configuration and request shapes